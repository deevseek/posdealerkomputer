# Environment Variables for LaptopPOS Local Development

# Database Configuration
# Replace with your local PostgreSQL database URL
DATABASE_URL=postgresql://username:password@localhost:5432/laptoppos_db

# PostgreSQL Connection Details (if needed separately)
PGHOST=localhost
PGPORT=5432
PGUSER=username
PGPASSWORD=password
PGDATABASE=laptoppos_db

# Application Configuration
NODE_ENV=development
PORT=5000

# Session Security
# Generate a strong random string for production
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Optional: Object Storage (if using cloud storage)
# GOOGLE_CLOUD_PROJECT_ID=your-project-id
# GOOGLE_CLOUD_BUCKET_NAME=your-bucket-name
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Optional: WhatsApp Integration (if needed)
# WHATSAPP_SESSION_PATH=./auth_info_baileys

# Development Settings
VITE_API_URL=http://localhost:5000